apiVersion: v1
kind: Service
metadata:
  name: fitness-service
spec:
  type: NodePort # This makes the app accessible to you
  selector:
    app: aceest-fitness
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30001 # You will access the app at this port